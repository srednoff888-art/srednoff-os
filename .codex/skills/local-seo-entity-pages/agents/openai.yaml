interface:
  display_name: "Local SEO Entity Pages"
  short_description: "SEO workflow for Local SEO Entity Pages"
  default_prompt: "Use $local-seo-entity-pages to handle this task with a production-ready workflow."

policy:
  allow_implicit_invocation: true
