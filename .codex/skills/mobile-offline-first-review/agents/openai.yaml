interface:
  display_name: "Mobile Offline First Review"
  short_description: "Mobile workflow for Mobile Offline First Review"
  default_prompt: "Use $mobile-offline-first-review to handle this task with Srednoff OS v2.1.1."

policy:
  allow_implicit_invocation: true