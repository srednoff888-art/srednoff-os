interface:
  display_name: "PPC Landing Quality Score Gate"
  short_description: "Growth workflow for PPC Landing Quality Score Gate"
  default_prompt: "Use $ppc-landing-quality-score-gate to handle this task with Srednoff OS v2.1.1."

policy:
  allow_implicit_invocation: true