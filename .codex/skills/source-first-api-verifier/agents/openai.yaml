interface:
  display_name: "Source First API Verifier"
  short_description: "Programming workflow for Source First API Verifier"
  default_prompt: "Use $source-first-api-verifier to handle this task with Srednoff OS v2.1.1."

policy:
  allow_implicit_invocation: true