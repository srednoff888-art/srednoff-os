interface:
  display_name: "Performance SRE Agent"
  short_description: "Agent Profile workflow for Performance SRE Agent"
  default_prompt: "Use $performance-sre-agent to handle this task with a production-ready workflow."

policy:
  allow_implicit_invocation: true
