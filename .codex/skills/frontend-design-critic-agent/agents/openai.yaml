interface:
  display_name: "Frontend Design Critic Agent"
  short_description: "Agent Profile workflow for Frontend Design Critic Agent"
  default_prompt: "Use $frontend-design-critic-agent to handle this task with a production-ready workflow."

policy:
  allow_implicit_invocation: true
