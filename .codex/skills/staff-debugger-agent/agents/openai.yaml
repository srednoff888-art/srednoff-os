interface:
  display_name: "Staff Debugger Agent"
  short_description: "Agent Profile workflow for Staff Debugger Agent"
  default_prompt: "Use $staff-debugger-agent to handle this task with a production-ready workflow."

policy:
  allow_implicit_invocation: true
