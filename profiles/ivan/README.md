# ivan

Sanitized maintainer profile example.

This directory must not contain Ivan's local paths, connector state, client data, private repositories, or secrets. Real local overrides should be stored outside the public repository.
